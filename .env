# Copy to .env and adjust as needed
BASE_URL=https://parabank.parasoft.com
FALLBACK_TO_ACCOUNT=12345

# 32-byte key for credential encryption (AES-256-GCM)
# Example (hex, 64 chars): generate with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
SECRET_KEY=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
